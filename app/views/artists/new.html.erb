<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create An Artist</title>
  </head>
  <body>
    <%= form_for Artist.new do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <%= f.label :bio %>
      <%= f.text_field :bio %>
      <%= f.submit %>
    <% end %>
  </body>
</html>
